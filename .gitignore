# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# JetBrains (project-wide; per-module ignores live in ui/ and api/)
.idea/
*.iml

# Local scratch
/tmp/
/scratch/

# Local secrets for the top-level docker-compose.yml — never commit, see .env.example
/.env
